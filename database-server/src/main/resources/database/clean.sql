
drop table if exists identity_sample, sample_tag, tag, sample, instrument,
    key, category, pack, genre, identity_producer_token, producer, label,
    identity_payment_token,  payment, identity, authority, image, subscription, plan, discount;